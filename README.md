# Koflow

Background jobs persistentes para Kof, com SQLite e execução no JVM.

Requisitos: Linux x86_64, Bash, Make, curl e tar. A distribuição Kof 0.3.22-beta
inclui o JDK 21; o driver SQLite JDBC usado é o 3.53.4.0.

```sh
make bootstrap
```
